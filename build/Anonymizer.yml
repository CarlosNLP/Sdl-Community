trigger:
  branches:
    include:
     - master
  paths:
    include:
     - Sdl.Community.UpLiftAutomationV4/*

pr:
  branches:
    include:
     - master
  paths:
    include:
     - Sdl.Community.UpLiftAutomationV4/*

pool: mypool

variables:
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'
  solution : '**/Sdl.Community.FragmentAlignmentAutomation.sln'
  pluginName: 'Anonymizer\Anonymizer'
  artifactName: 'Sdl.Community.projectAnonymizer.sdlplugin'

steps:
  - template: jobs/build-sdlplugin.yml